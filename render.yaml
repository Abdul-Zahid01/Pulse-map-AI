services:
  - type: web
    name: pulse-map-pdx
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: APP_URL
        sync: false
      - key: NODE_ENV
        value: production
